query GetDocumentReferenceUrl($subject: string, $related: string) {
  DocumentReferenceList(subject: "{subject}", related: "{related}") {
    content {
      attachment {
        url
      }
    }
  }
}